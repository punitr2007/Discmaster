# DiscMaster Python Dependencies
# ──────────────────────────────
# Runtime Core:
# DiscMaster is engineered to run with zero mandatory third-party packages,
# using the Python standard library (tkinter, threading, subprocess, json, struct, etc.).
#
# Ensure system-level dependencies are installed on Linux:
# - Tkinter: `sudo apt install python3-tk` or `sudo pacman -S tk`
# - FFmpeg:  `sudo apt install ffmpeg`     or `sudo pacman -S ffmpeg`

# Optional GUI & Image Enhancements:
Pillow>=10.0.0

# Optional Packaging & Standalone Binary Building:
pyinstaller>=6.0.0

# Optional Testing & Quality Assurance:
pytest>=7.4.0
flake8>=6.1.0
